# Nested AGENTS Auth Subtree

Launch Codex from this directory when validating that the nearer `services/api/AGENTS.override.md` file overrides the broader root policy for this scope.

This subtree also contains the in-scope `AuthHandler.cs` sample file used by the nested active-analysis eval case. A sibling web-handler fixture exists only to prove that scope containment excludes files outside this subtree.
